const number = [1,2,3,4,5,6,7];

//      usamos numbers.length
for ( let i = 0; i < number.length; i ++ ){
    /*
    cramos la varialbe elment que guarde el valor de numbers [i]
    (cada elemento del aray en cada vuelta del mismo)
    simplemente por comodidad a la hroa de continuar con el código
    */
   const elemtn = numbers [i];

   console.log (element);
}